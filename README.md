Sources for the plugin `plugin.applovinMax.AdColony`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.AdColony" = {
            publisherId = "com.solar2d",
        },
    },
}
```
